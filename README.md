# A simple carousel.

## The carousel accepts the following props:
### — pagination (boolean), 
### — navigation (boolean),  
### — slidesPerView (number),   
// Fractional value breaks pagination // 
### — gap (number), 
### — and of course, children (ReactNode). 